[build]
  command = "npm run build:netlify"
  publish = ".netlify/static"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

[[edge_functions]]
  function = "server"
  path = "/*"
